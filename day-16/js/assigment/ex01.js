// ogogdson 2 toonii ihiig oloh punkts]

let num1 = 500;
let num2 = 600_000; 

function findMax (x, y) {
    if (x < y) {
        console.log(y + " is bigger then " + x);
    } else {
        console.log(x + " is bigger then " + y);
    }
}

findMax(num1, num2)